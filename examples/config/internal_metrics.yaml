sources:
  metrics:
    type: internal_metrics

transforms:
  add_tags:
    type: add_tags
    inputs:
      - metrics
    tags:
      host: ${HOSTNAME}

sinks:
  prom:
    type: prometheus_exporter
    inputs:
      - add_tags