[package]
name = "serde_regex"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
regex = { version = "1.7.1" }
serde = { version = "1.0.154", default-features = false, features = ["derive"] }