[package]
name = "serde_regex"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
regex = { version = "1.7.0" }
serde = { version = "1.0.147", default-features = false, features = ["derive"] }