[package]
name = "ntp"
version = "0.1.0"
edition = "2021"
publish = false

[dev-dependencies]
tokio = { version = "1.35.1", default-features = false, features = ["net", "rt-multi-thread", "macros"] }

[dependencies]
chrono = { version = "0.4.32", default-features = false, features = ["clock"] }
rand = { version = "0.8.5", default-features = false, features = ["std_rng", "std"] }
tokio = { version = "1.35.1", default-features = false, features = ["net"] }
