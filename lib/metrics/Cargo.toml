[package]
name = "metrics"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
parking_lot = { version = "0.12.3", default-features = false }

[dev-dependencies]
criterion = { version = "0.6.0", default-features = false }

[[bench]]
name = "register"
harness = false