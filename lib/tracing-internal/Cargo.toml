[package]
name = "tracing-internal"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
event = { path = "../event" }
tracing = { version = "0.1.44", default-features = false, features = ["attributes", "std"] }
tracing-core = { version = "0.1.35", default-features = false }
tracing-subscriber = { version = "0.3.22", default-features = false, features = ["registry", "std"] }
