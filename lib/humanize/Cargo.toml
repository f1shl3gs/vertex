[package]
name = "humanize"
version = "0.1.0"
edition = "2021"

[[bench]]
name = "duration"
harness = false
path = "benches/duration.rs"

[dev-dependencies]
criterion = { version = "0.4.0" }

[dependencies]
serde = { version = "1.0.147", default-features = false, features = ["std"] }