[package]
name = "tripwire"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]

[dev-dependencies]
futures = { version = "0.3.30", default-features = false }
tokio = { version = "1.35.1", default-features = false, features = ["test-util", "macros", "rt-multi-thread"] }
tokio-stream = { version = "0.1.14" }
