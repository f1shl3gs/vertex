[package]
name = "tripwire"
version = "0.1.0"
edition = "2024"
publish = false

[dev-dependencies]
futures = { version = "0.3.33", default-features = false, features = ["async-await"] }
tokio = { version = "1.53.1", default-features = false, features = ["test-util", "macros", "rt-multi-thread"] }
tokio-stream = { version = "0.1.19", default-features = false }

[dependencies]
