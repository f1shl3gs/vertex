[package]
name = "tripwire"
version = "0.1.0"
edition = "2024"
publish = false

[dev-dependencies]
futures = { version = "0.3.32", default-features = false, features = ["async-await"] }
tokio = { version = "1.49.0", default-features = false, features = ["test-util", "macros", "rt-multi-thread"] }
tokio-stream = { version = "0.1.18", default-features = false }

[dependencies]
