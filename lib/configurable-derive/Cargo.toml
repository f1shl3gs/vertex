[package]
name = "configurable-derive"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
proc-macro = true

[dependencies]
proc-macro2 = { version = "1.0.52", default-features = false }
quote = { version = "1.0.26", default-features = false }
syn = { version = "2.0.10", default-features = false, features = [ "parsing", "proc-macro", "derive", "extra-traits", "full" ] }

[dev-dependencies]
configurable = { path = "../configurable" }
inventory = "0.3.4"
