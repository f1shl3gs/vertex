[package]
name = "file-source"
version = "0.1.0"
edition = "2018"

[dependencies]
bstr = { version = "0.2.17", default-features = false, features = [] }
crc = "2.0.0"
glob = "0.3.0"

bytes = { version = "1.1.0", default-features = false, features = [] }