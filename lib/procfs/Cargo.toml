[package]
name = "procfs"
version = "0.1.0"
edition = "2021"
publish = false
exclude = ["fixtures"]

[dev-dependencies]
tokio = { version = "1.15.0", features = ["fs", "io-util", "test-util", "macros"] }

[dependencies]
glob = "0.3.0"
tokio = { version = "1.15.0", features = ["fs", "io-util"] }