index = 1.1
item = "z"
array = ["a", "b", "c"]
for index, item in array {
    index = item
}

assert_eq(index, 1.1)
assert_eq(item, "z")


key = 1.1
value = false
map = {
    "k1": "v1",
    "k2": "v2"
}
for key, value in map {
    key = value
}

assert_eq(key, 1.1)
assert_eq(value, false)
