[package]
name = "condition"
version = "0.1.0"
edition = "2021"
publish = false

[[bench]]
name = "eval"
harness = false

[dependencies]
event = { path = "../event" }
lookup = { path = "../lookup" }
regex = { version = "1.8.4" }
serde = { version = "1.0.166" }

[dev-dependencies]
criterion = { version = "0.4.0", default-features = false }